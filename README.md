# Better project

Work In Progress!

Set of jobs to calculate what I should bet on.


## How to run:

```sh
$ ELASTICSEARCH_HOST="{es_host}" python run.py {task}
```

List of tasks are in better/tasks.


## Deploy to GCP/KGE:

There are all needed deploy values in the directory deploy.  

## Update requirements.txt dependency libraries

```sh
# make pip-update
```